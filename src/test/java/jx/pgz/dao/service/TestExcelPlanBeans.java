package jx.pgz.dao.service;

import org.junit.jupiter.api.Test;

import java.io.IOException;

public class TestExcelPlanBeans {
    @Test
    void test1() throws IOException {
//        List<PlanBeanExcel> planBeanExcels = JSON.parseArray(FileUtil.readUtf8String(new File("C:\\Users\\pengmaofang\\Desktop\\baobiao\\src\\main\\resources\\eg.json")), PlanBeanExcel.class);
//        for (PlanBeanExcel planBeanExcel : planBeanExcels) {
//            for (int i = 0; i < planBeanExcel.getRobotExInfos().size(); i++) {
//                planBeanExcel.getRobotExInfos().get(i).setNo(i+1);
//            }
//        }
//
//
//
//        Workbook workbook = ExcelExportUtil.exportExcel(new ExportParams(),
//                PlanBeanExcel.class, planBeanExcels);
//
//        workbook.write(new FileOutputStream(new File("C:\\Users\\pengmaofang\\Desktop\\nx\\de.xls")));
//        workbook.close();
    }
}
